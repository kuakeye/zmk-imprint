on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Phase 2: back on the current reusable workflow. The zephyr-4.1 board uses
    # the new hardware-model-v2 layout, so the board-variant compat check that
    # crashed on the old Nov-2025 board format is satisfied here.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
