on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Pinned to the reusable workflow as of the last known-good build (2025-11-17,
    # same commit as the zmk pin in config/west.yml). The @main version added a
    # Zephyr-4.1 "board variant" compat check that crashes (KeyError: 'qualifiers')
    # on the Nov-2025 assimilator-bt board format, which fails the job and skips
    # artifact upload even though the firmware itself builds fine.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@e34793e8c7193bc868b95ec38d78f67956f759cb
